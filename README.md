je veux créer un programme écrit en python, html et javascript

le système est basé sur un serveur http via la librairie cheerypy.

le système gère une liste de noms qui sont associés à autant d'utilisateurs

quand un utilisateur se connecte au serveur ( point d'entree 'enter') , une page présente les noms de la liste et il est invité à clicker sur son nom.

cette info est enregistrée dans un cookie pour lui éviter de se ré-identifier.

Ensuite, une nouvelle page présente  