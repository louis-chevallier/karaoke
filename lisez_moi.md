# Démonstration CherryPy + Tkinter

Installation:
```
pip install -r requirements.txt
python server.py
```

Ouvrir ensuite http://localhost:8080

Le cookie mémorise l'utilisateur. Un clic sur Oui/Non fait clignoter son nom dans la fenêtre Tkinter (vert=Oui, rouge=Non).
